declare global {
  namespace ReactNavigation {
    interface Theme {
      colors: {
        primary: string;
        background: string;
        card: string;
        text: string;
        border: string;
        notification: string;
        icon: string;
        tint: string;
      };
    }
  }
}

export { };
